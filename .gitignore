node_modules/
dist/
build/
release/
out/

# electron-builder buildResources dir — committed (installer.nsh, icons)
!desktop/build/
!desktop/build/**
*.log
logs/
.env
.env.local
.DS_Store
.vite/
coverage/
*.tsbuildinfo

# runtime config written by the setup wizard / on the control PC — never committed
config/excontrol.config.json
config/*.local.json
desktop/release/
desktop/out/
